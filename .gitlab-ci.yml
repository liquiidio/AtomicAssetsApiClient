image : registry.gitlab.com/liquiduniverse/docgen:latest

stages:
    #- build
   # - build-core
    #- test
   # - test-core
    - pages
    - update-parent

#build:
    #stage: build
    #script:
       # - dotnet build
   # artifacts:
        #paths:
          #  - ./build/
 
#build-core:
    #stage: build-core
   # script:
       # - dotnet build
    #artifacts:
       # paths:
           # - ./build/

#test:
    #stage: test
   # script:
        #- dotnet test AtomicAssetsApiClient.Test/AtomicAssetsApiClient.Test.csproj

#test-core:
    #stage: test-core
    #script:
        #- dotnet test AtomicAssetsApiClient.Core.Test/AtomicAssetsApiClient.Core.Test.csproj

pages:
    stage: pages
    script:
        - doxygen documentation/doxygen/Doxyfile
        - doxybook2 --input  documentation/doxyoutput/xml --output  documentation/gitbook --config documentation/doxybook-config/gitbook/.doxybook/config.json --summary-input documentation/doxybook-config/gitbook/SUMMARY.md.tmpl --summary-output documentation/gitbook/SUMMARY.md 
        - gitbook build documentation/gitbook documentation/gitbook_output
        - cp -r documentation/gitbook_output public
    
  
    artifacts:
      paths:
       - public
       - documentation

update-parent:
    stage: update-parent
    script:
    - git clone https://Ash_Lee:CI_TOKEN@gitlab.com/liquiduniverse/unityeosclientplugins/theliquiidbible_gitbook.git
    - mkdir -p theliquiidbible_gitbook/documentation/gitbook_output
    - cp -r documentation/gitbook_output  theliquiidbible_gitbook/documentation/gitbook_output
    - cd theliquiidbible_gitbook
    - git config --global user.email "ashleewawa17@gmail.com"
    - git config --global user.name "Ash_lee"
    - git add .
    - git commit -m"Updating"
    - git push origin HEAD wax-developer
